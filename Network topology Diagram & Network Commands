       ┌───────────────┐
       │  public_net   │
       │ Traefik/Nginx │
       └─────┬─────────┘
             │
             ▼
       ┌───────────────┐
       │ frontend_net  │
       │ Frontend      │
       │ Backend API   │
       └─────┬─────────┘
             │
             ▼
       ┌───────────────┐
       │ backend_net   │
       │ Backend API   │
       │ Database      │
       └───────────────┘
       
       ┌───────────────┐
       │ monitoring_net│
       │ Prometheus    │
       │ Grafana       │
       └───────────────┘

docker network create -d overlay --attachable --opt encrypted public_net
docker network create -d overlay --attachable --opt encrypted frontend_net
docker network create -d overlay --attachable --opt encrypted backend_net
docker network create -d overlay --opt encrypted monitoring_net

